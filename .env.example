# Backend API Configuration
# For local development without Docker, uncomment to use direct backend URL:
# VITE_API_BASE_URL=http://localhost:8090/api/v1
# For Docker/production, leave unset to use relative path /api/v1 (nginx proxy)

# Development settings
VITE_NODE_ENV=development